namespace StageFright.Core.Enums;

/// <summary>Horizontal alignment for a report column.</summary>
public enum ReportColumnAlignment
{
    /// <summary>Left-aligned text (default for strings).</summary>
    Left,

    /// <summary>Centre-aligned text.</summary>
    Center,

    /// <summary>Right-aligned text (default for numbers and currency).</summary>
    Right
}
