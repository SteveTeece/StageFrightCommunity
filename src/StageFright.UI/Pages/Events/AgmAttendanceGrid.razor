@if (_rows.Count == 0)
{
    <div class="alert alert-warning py-1 mb-2 small">No active members found.</div>
}
else
{
    <div class="agm-attendance-scroll" style="flex:1; min-height:0; overflow-y:auto;">
        <RadzenDataGrid Data="@_rows" TItem="AttendanceRow"
                        AllowSorting="true" AllowPaging="false"
                        class="rz-shadow-0">
            <Columns>
                <RadzenDataGridColumn TItem="AttendanceRow" Title="Attended" Width="120px" Sortable="false">
                    <HeaderTemplate>
                        <div class="d-flex flex-column align-items-center gap-1">
                            <span>Attended</span>
                            <span class="select-all-inline small text-muted">
                                (<input type="checkbox" class="form-check-input" id="select-all-agm-attended"
                                        checked="@AllAttended"
                                        @onchange="e => { ToggleSelectAll((bool)(e.Value ?? false)); StateHasChanged(); }"
                                        aria-label="Select all members as attended" />
                                <label class="form-check-label mb-0" for="select-all-agm-attended">Select All</label>)
                            </span>
                        </div>
                    </HeaderTemplate>
                    <Template Context="row">
                        <div class="text-center">
                            <input type="checkbox" class="form-check-input" id="agm-attended-@row.MemberId"
                                   checked="@row.Attended"
                                   @onchange="e => { row.Attended = (bool)(e.Value ?? false); StateHasChanged(); }"
                                   aria-label="@row.MemberName attended" />
                        </div>
                    </Template>
                </RadzenDataGridColumn>
                <RadzenDataGridColumn TItem="AttendanceRow" Property="MemberName" Title="Member" />
            </Columns>
        </RadzenDataGrid>
    </div>
}
