@inherits ComponentBase

@if (_loading)
{
    <span class="text-muted small">@Shared["Shared_Common_Loading"]</span>
}
else if (_error)
{
    <span class="text-danger small">@Shared["Shared_Tile_LoadError"]</span>
}
else if (!_hasData)
{
    <span class="text-muted small">@L["Rehearsals_TrendTile_Empty"]</span>
}
else
{
    <LineChart @ref="_chart" Height="110" />
}
