# Tasks: Past AGM Committee List & Print Report

- [X] **T001** [US1] In `BuildPositionLines`, stop appending the "General Committee Member" line; add a new `GeneralCommitteeMemberNames` property that returns the sorted plain-name list for positions with `OfficeHolderTypeId is null` (FR-001, FR-003) + `src/StageFright.UI/Pages/Events/AgmDetail.razor.cs`
- [X] **T002** [US1] After the `PositionLines` `<ul>`, render a `BorderedListBox<string>` (one row per name) under a "General Committee Member" label when `GeneralCommitteeMemberNames.Count > 0`; render nothing when empty; keep the "No positions recorded." fallback but trigger it only when both `PositionLines` and `GeneralCommitteeMemberNames` are empty (FR-001–FR-003) + `src/StageFright.UI/Pages/Events/AgmDetail.razor`
- [X] **T003** [US1] Extend `AgmDetailTests` with cases for: three general committee members render as three separate list-box rows (not comma-joined), a single member renders as one row, zero members omits the list box entirely, and named office-holder positions are still rendered unchanged + `tests/StageFright.UI.Tests/Pages/Events/AgmDetailTests.cs`
- [X] **T004** [P] [US2] Add `AgmResultsPositionLine` (Label, MemberText) — sealed, init-only, one class per file, mirroring `AgmAttendanceSheetMember` + `src/StageFright.Core/Modules/Agm/AgmResultsPositionLine.cs`
- [X] **T005** [P] [US2] Add `AgmResultsData` (AgmDate, AttendedCount, TotalCount, `IReadOnlyList<AgmResultsPositionLine>` PositionLines, `IReadOnlyList<string>` GeneralCommitteeMemberNames) — sealed, init-only, mirroring `AgmAttendanceSheetData` + `src/StageFright.Core/Modules/Agm/AgmResultsData.cs`
- [X] **T006** [US2] Add `IAgmResultsPdfRenderer` with `byte[] Render(AgmResultsData data, string organizationName = "")` (depends on T004, T005) + `src/StageFright.Reports/Rendering/IAgmResultsPdfRenderer.cs`
- [X] **T007** [US2] Implement `AgmResultsPdfRenderer` with QuestPDF directly (not `CheckboxSheetPdfBuilder` — this isn't a checkbox roll): header shows org name / "AGM Results" / AGM date / attendance count; body lists each named position line, then "General Committee Member" plus one line per name, or "No positions recorded." when both are empty; footer shows page numbers — follow `PdfReportRenderer`'s page setup (FR-005, FR-006) (depends on T006) + `src/StageFright.Reports/Rendering/AgmResultsPdfRenderer.cs`
- [X] **T008** [US2] Register `IAgmResultsPdfRenderer`/`AgmResultsPdfRenderer` in DI next to `IAgmAttendanceSheetPdfRenderer` (depends on T007) + `src/StageFright.App/MauiProgram.cs`
- [X] **T009** [US2] Inject `IAgmResultsPdfRenderer`, add a `PrintAgmResults` handler that builds `AgmResultsData` from the already-loaded `_agm`/`_attendance`/`PositionLines`/`GeneralCommitteeMemberNames`, renders to a temp PDF, and opens it via `Process.Start` — copy `PrintAttendanceReport`'s try/catch-and-inline-warning shape exactly (FR-004, FR-007, FR-008) (depends on T001, T008) + `src/StageFright.UI/Pages/Events/AgmDetail.razor.cs`
- [X] **T010** [US2] Add a second header button ("Print AGM Results") next to the existing "Print Attendance Report" button, wired to `PrintAgmResults` (depends on T009) + `src/StageFright.UI/Pages/Events/AgmDetail.razor`
- [X] **T011** [P] [US2] Add `AgmResultsPdfRendererTests` mirroring `AgmAttendanceSheetPdfRendererTests` — non-null/non-empty bytes for populated, zero-position, and empty-organization-name input; no exception thrown (depends on T007) + `tests/StageFright.Reports.Tests/AgmResultsPdfRendererTests.cs`
- [X] **T012** [US2] Extend `AgmDetailTests` for the new button: renders on load, service-throws-equivalent failure path shows the inline warning and never calls the mocked renderer's `Render`, and that clicking one print button never touches the other renderer mock (FR-007, FR-008) (depends on T009, T010) + `tests/StageFright.UI.Tests/Pages/Events/AgmDetailTests.cs`
- [X] **T013** Add `AgmResultsPdfRenderer` to the Reports-pipeline paragraph's list of print-only sheets built outside `IReportProvider`, alongside `AgmAttendanceSheetPdfRenderer` + `CLAUDE.md`
- [ ] **T014** *(deferred — not performed this session)* Launch the app and visually verify, in light theme, that a recorded AGM with several general committee members and at least one named position shows the list box (one name per line) and that both print buttons each open a distinct, correct PDF + `src/StageFright.UI/Pages/Events/AgmDetail.razor`
- [ ] **T015** *(deferred — not performed this session)* Repeat the visual verification in dark theme + `src/StageFright.UI/Pages/Events/AgmDetail.razor`
- [X] **T016** Run a full `dotnet build` and `dotnet test` and report the results per the project's build/test verification rule + `StageFrightCommunity.slnx`
